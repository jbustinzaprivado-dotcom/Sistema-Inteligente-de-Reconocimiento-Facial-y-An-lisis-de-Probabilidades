# URL del backend en produccion (Render), incluyendo /api al final.
# En desarrollo local se puede dejar vacia/sin definir: se usa el proxy de Vite hacia localhost:8000.
VITE_API_URL=https://tu-backend.onrender.com/api